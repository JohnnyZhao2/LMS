# LMS 学习管理系统 - 前端项目架构设计

## 技术栈

- **框架**: React 18 + TypeScript
- **路由**: React Router v6
- **状态管理**: Zustand
- **UI 组件库**: Ant Design 5.x
- **HTTP 客户端**: Axios
- **构建工具**: Vite
- **代码规范**: ESLint + Prettier + Husky

---

## 项目结构总览

```
src/
├── app/                          # 应用层：路由、布局、提供者
│   ├── layouts/                  # 布局组件
│   │   ├── main-layout.tsx       # 主布局（含侧边栏、顶部栏）
│   │   ├── auth-layout.tsx       # 登录页布局
│   │   └── exam-layout.tsx       # 考试专用布局（全屏、防作弊）
│   ├── providers/                # 全局 Provider
│   │   ├── app-provider.tsx      # 应用主 Provider
│   │   ├── auth-provider.tsx     # 认证 Provider
│   │   └── theme-provider.tsx    # 主题 Provider
│   ├── routes/                   # 路由配置
│   │   ├── index.tsx             # 路由入口
│   │   ├── protected-route.tsx   # 权限路由守卫
│   │   └── role-routes.tsx       # 角色路由映射
│   └── app.tsx                   # 应用入口组件
│
├── assets/                       # 静态资源
│   ├── images/                   # 图片资源
│   ├── icons/                    # 图标资源
│   └── fonts/                    # 字体文件
│
├── components/                   # 全局共享组件
│   ├── ui/                       # 基础 UI 组件
│   │   ├── button/
│   │   ├── card/
│   │   ├── modal/
│   │   ├── table/
│   │   └── form/
│   ├── layout/                   # 布局相关组件
│   │   ├── sidebar/
│   │   ├── header/
│   │   └── page-header/
│   ├── data-display/             # 数据展示组件
│   │   ├── stat-card/            # 统计卡片
│   │   ├── progress-card/        # 进度卡片
│   │   └── empty-state/          # 空状态
│   ├── feedback/                 # 反馈组件
│   │   ├── loading/
│   │   ├── error-boundary/
│   │   └── notification/
│   └── business/                 # 业务通用组件
│       ├── role-switcher/        # 角色切换器 ⭐
│       ├── user-selector/        # 人员选择器 ⭐
│       ├── tag-filter/           # 多级标签筛选器 ⭐
│       └── rich-editor/          # 富文本编辑器
│
├── config/                       # 全局配置
│   ├── index.ts                  # 配置入口
│   ├── constants.ts              # 常量定义
│   ├── api-endpoints.ts          # API 端点配置
│   └── roles.ts                  # 角色权限配置
│
├── features/                     # 特性模块（核心业务逻辑）
│   │
│   ├── auth/                     # 🔐 认证模块
│   │   ├── api/
│   │   │   └── auth-api.ts       # 登录/登出 API
│   │   ├── components/
│   │   │   ├── login-form.tsx
│   │   │   └── role-guard.tsx
│   │   ├── hooks/
│   │   │   └── use-auth.ts
│   │   ├── stores/
│   │   │   └── auth-store.ts     # 用户信息、当前角色
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx             # 模块入口（登录页）
│   │
│   ├── dashboard/                # 📊 仪表盘模块
│   │   ├── api/
│   │   │   └── dashboard-api.ts
│   │   ├── components/
│   │   │   ├── student/          # 学员仪表盘组件
│   │   │   │   ├── todo-list.tsx
│   │   │   │   ├── latest-knowledge.tsx
│   │   │   │   └── stats-overview.tsx
│   │   │   ├── mentor/           # 导师/室经理仪表盘组件
│   │   │   │   ├── completion-stats.tsx
│   │   │   │   ├── pending-grading.tsx
│   │   │   │   └── quick-actions.tsx
│   │   │   └── admin/            # 管理员仪表盘组件
│   │   │       └── platform-stats.tsx
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── knowledge/                # 📚 知识中心模块
│   │   ├── api/
│   │   │   ├── knowledge-api.ts  # 知识文档 CRUD
│   │   │   └── tags-api.ts       # 标签管理
│   │   ├── components/
│   │   │   ├── knowledge-list/   # 知识列表（卡片样式）
│   │   │   ├── knowledge-detail/ # 知识详情（含目录、完成按钮）
│   │   │   ├── knowledge-filter/ # 多级标签筛选
│   │   │   ├── knowledge-editor/ # 知识编辑器（管理员）
│   │   │   └── tag-manager/      # 标签管理（管理员）
│   │   ├── hooks/
│   │   │   ├── use-knowledge-list.ts
│   │   │   └── use-knowledge-filter.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── tasks/                    # 📋 任务中心模块
│   │   ├── api/
│   │   │   └── task-api.ts       # 任务 CRUD
│   │   ├── components/
│   │   │   ├── task-list/        # 任务列表（卡片样式）
│   │   │   ├── task-detail/      # 任务详情
│   │   │   ├── task-create/      # 新建任务向导
│   │   │   │   ├── step-type-select.tsx    # Step1: 选择类型
│   │   │   │   ├── step-learning-form.tsx  # Step2a: 学习任务表单
│   │   │   │   ├── step-practice-form.tsx  # Step2b: 练习任务表单
│   │   │   │   ├── step-exam-form.tsx      # Step2c: 考试任务表单
│   │   │   │   └── step-user-select.tsx    # Step3: 人员选择
│   │   │   ├── task-filter/      # 任务筛选
│   │   │   └── task-progress/    # 任务完成进度
│   │   ├── hooks/
│   │   │   ├── use-task-list.ts
│   │   │   └── use-task-create.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── quiz/                     # 📝 测验/试卷模块
│   │   ├── api/
│   │   │   └── quiz-api.ts       # 测验 CRUD
│   │   ├── components/
│   │   │   ├── quiz-list/        # 测验列表
│   │   │   ├── quiz-editor/      # 组卷编辑器 ⭐
│   │   │   │   ├── question-picker.tsx   # 从题库选题
│   │   │   │   ├── question-creator.tsx  # 新建题目
│   │   │   │   ├── question-sorter.tsx   # 题目排序
│   │   │   │   └── score-setter.tsx      # 分数设置
│   │   │   └── quiz-preview/     # 试卷预览
│   │   ├── hooks/
│   │   │   └── use-quiz-editor.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── exam/                     # ✍️ 答题模块
│   │   ├── api/
│   │   │   ├── exam-api.ts       # 答题提交
│   │   │   └── answer-api.ts     # 答案保存
│   │   ├── components/
│   │   │   ├── exam-taking/      # 答题界面 ⭐
│   │   │   │   ├── question-nav.tsx      # 题目导航
│   │   │   │   ├── question-display.tsx  # 题目展示
│   │   │   │   ├── answer-input.tsx      # 答案输入
│   │   │   │   ├── timer.tsx             # 倒计时
│   │   │   │   └── submit-confirm.tsx    # 交卷确认
│   │   │   ├── exam-result/      # 结果展示
│   │   │   │   ├── score-summary.tsx     # 分数汇总
│   │   │   │   ├── answer-review.tsx     # 答案回顾
│   │   │   │   └── analysis-panel.tsx    # 解析面板
│   │   │   └── exam-notice/      # 考试须知
│   │   ├── hooks/
│   │   │   ├── use-exam-timer.ts
│   │   │   └── use-answer-state.ts
│   │   ├── stores/
│   │   │   └── exam-store.ts     # 答题状态管理
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── grading/                  # ✅ 评分中心模块
│   │   ├── api/
│   │   │   └── grading-api.ts
│   │   ├── components/
│   │   │   ├── pending-list/     # 待评分列表
│   │   │   ├── grading-panel/    # 评分界面（左右分栏）
│   │   │   │   ├── answer-display.tsx    # 答案展示
│   │   │   │   ├── score-input.tsx       # 分数输入
│   │   │   │   └── quick-actions.tsx     # 一键满分等
│   │   │   └── graded-list/      # 已评分列表
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── spot-check/               # 🔍 抽查中心模块
│   │   ├── api/
│   │   │   └── spot-check-api.ts
│   │   ├── components/
│   │   │   ├── check-form/       # 抽查录入表单
│   │   │   └── check-history/    # 历史记录
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── question-bank/            # 🗃️ 题库管理模块（管理员）
│   │   ├── api/
│   │   │   └── question-api.ts
│   │   ├── components/
│   │   │   ├── question-list/    # 题目列表
│   │   │   ├── question-editor/  # 题目编辑
│   │   │   ├── question-import/  # Excel 批量导入
│   │   │   └── question-filter/  # 题目筛选
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── users/                    # 👥 用户与权限模块（管理员）
│   │   ├── api/
│   │   │   ├── user-api.ts
│   │   │   └── org-api.ts
│   │   ├── components/
│   │   │   ├── user-list/        # 用户列表
│   │   │   ├── user-form/        # 用户表单
│   │   │   ├── org-tree/         # 组织架构树
│   │   │   └── mentor-bindng/    # 师徒关系绑定
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   ├── analytics/                # 📈 数据看板模块（团队经理）
│   │   ├── api/
│   │   │   └── analytics-api.ts
│   │   ├── components/
│   │   │   ├── dept-comparison/  # 各室对比分析
│   │   │   ├── knowledge-heat/   # 知识库热度
│   │   │   └── employee-rank/    # 员工排行榜
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── index.tsx
│   │
│   └── profile/                  # 👤 个人中心模块（仅学员）
│       ├── api/
│       │   └── profile-api.ts
│       ├── components/
│       │   ├── user-info/        # 用户信息
│       │   ├── credit-record/    # 积分/学分记录
│       │   └── wrong-book/       # 错题本
│       ├── types/
│       │   └── index.ts
│       └── index.tsx
│
├── hooks/                        # 全局共享 Hooks
│   ├── use-pagination.ts         # 分页
│   ├── use-debounce.ts           # 防抖
│   ├── use-local-storage.ts      # 本地存储
│   └── use-media-query.ts        # 响应式
│
├── lib/                          # 预配置库
│   ├── axios.ts                  # Axios 实例配置
│   ├── dayjs.ts                  # 日期处理配置
│   └── react-query.ts            # React Query 配置
│
├── stores/                       # 全局状态存储
│   ├── ui-store.ts               # UI 状态（侧边栏、主题）
│   └── notification-store.ts     # 通知状态
│
├── types/                        # 共享 TypeScript 类型
│   ├── api.ts                    # API 响应类型
│   ├── user.ts                   # 用户相关类型
│   ├── task.ts                   # 任务相关类型
│   ├── question.ts               # 题目相关类型
│   └── common.ts                 # 通用类型
│
├── utils/                        # 共享工具函数
│   ├── format.ts                 # 格式化工具
│   ├── validation.ts             # 验证工具
│   ├── permission.ts             # 权限判断工具
│   └── storage.ts                # 存储工具
│
└── main.tsx                      # 应用入口
```

---

## 特性模块详解

### 模块划分依据

| 模块名 | 对应功能 | 角色访问权限 |
|--------|----------|--------------|
| `auth` | 登录、角色切换 | 所有用户 |
| `dashboard` | 仪表盘 | 所有用户（内容按角色区分） |
| `knowledge` | 知识中心 | 学员查看，管理员编辑 |
| `tasks` | 任务中心 | 学员查看，导师/室经理/管理员创建 |
| `quiz` | 测验/试卷管理 | 导师/室经理/管理员 |
| `exam` | 答题界面 | 学员（答题）、导师/室经理（预览） |
| `grading` | 评分中心 | 导师/室经理/管理员 |
| `spot-check` | 抽查中心 | 导师/室经理 |
| `question-bank` | 题库管理 | 管理员 |
| `users` | 用户与权限 | 管理员 |
| `analytics` | 数据看板 | 团队经理 |
| `profile` | 个人中心 | 仅学员 |

---

## 路由设计

```typescript
// src/app/routes/index.tsx

const routes = [
  // 公开路由
  { path: '/login', element: <LoginPage /> },
  
  // 学员路由
  { 
    path: '/student',
    element: <ProtectedRoute roles={['student']} />,
    children: [
      { path: 'dashboard', element: <StudentDashboard /> },
      { path: 'knowledge', element: <KnowledgeList /> },
      { path: 'knowledge/:id', element: <KnowledgeDetail /> },
      { path: 'tasks', element: <TaskList /> },
      { path: 'tasks/:id', element: <TaskDetail /> },
      { path: 'exam/:taskId/:quizId', element: <ExamTaking /> },
      { path: 'profile', element: <Profile /> },
    ]
  },
  
  // 导师路由
  {
    path: '/mentor',
    element: <ProtectedRoute roles={['mentor']} />,
    children: [
      { path: 'dashboard', element: <MentorDashboard /> },
      { path: 'quizzes', element: <QuizList /> },
      { path: 'quizzes/create', element: <QuizEditor /> },
      { path: 'quizzes/:id/edit', element: <QuizEditor /> },
      { path: 'tasks', element: <TaskManagement /> },
      { path: 'tasks/create', element: <TaskCreate /> },
      { path: 'grading', element: <GradingCenter /> },
      { path: 'grading/:id', element: <GradingPanel /> },
      { path: 'spot-check', element: <SpotCheckCenter /> },
    ]
  },
  
  // 室经理路由（与导师类似，数据范围不同）
  {
    path: '/manager',
    element: <ProtectedRoute roles={['department_manager']} />,
    children: [
      // 同导师路由结构
    ]
  },
  
  // 团队经理路由
  {
    path: '/team-manager',
    element: <ProtectedRoute roles={['team_manager']} />,
    children: [
      { path: 'dashboard', element: <AnalyticsDashboard /> },
    ]
  },
  
  // 管理员路由
  {
    path: '/admin',
    element: <ProtectedRoute roles={['admin']} />,
    children: [
      { path: 'dashboard', element: <AdminDashboard /> },
      { path: 'knowledge', element: <KnowledgeManagement /> },
      { path: 'knowledge/tags', element: <TagManagement /> },
      { path: 'question-bank', element: <QuestionBank /> },
      { path: 'quizzes', element: <QuizManagement /> },
      { path: 'tasks', element: <TaskManagement /> },
      { path: 'users', element: <UserManagement /> },
      { path: 'organization', element: <OrganizationManagement /> },
    ]
  }
];
```

---

## 核心组件设计

### 1. 角色切换器 (Role Switcher)

```typescript
// src/components/business/role-switcher/role-switcher.tsx

interface RoleSwitcherProps {
  currentRole: Role;
  availableRoles: Role[];
  onRoleChange: (role: Role) => void;
}

/**
 * 角色切换器组件
 * - 仅当用户拥有多个角色时显示
 * - 切换后实时刷新菜单和路由
 */
export const RoleSwitcher: React.FC<RoleSwitcherProps>;
```

### 2. 人员选择器 (User Selector)

```typescript
// src/components/business/user-selector/user-selector.tsx

interface UserSelectorProps {
  mode: 'mentor' | 'manager' | 'admin';  // 决定数据范围
  value: string[];                        // 选中的用户 ID
  onChange: (userIds: string[]) => void;
  excludeSelf?: boolean;                  // 是否过滤自己
}

/**
 * 人员选择器组件
 * - mentor: 仅显示我的学员
 * - manager: 显示本室全员，支持全选/按入职筛选
 * - admin: 显示全公司，按组织架构选择
 */
export const UserSelector: React.FC<UserSelectorProps>;
```

### 3. 多级标签筛选器 (Tag Filter)

```typescript
// src/components/business/tag-filter/tag-filter.tsx

interface TagFilterProps {
  primaryTags: Tag[];       // 一级标签（双云/网络/数据库...）
  secondaryTags: Tag[];     // 二级标签（动态加载）
  operationTags: Tag[];     // 操作标签
  value: TagFilterValue;
  onChange: (value: TagFilterValue) => void;
}

/**
 * 多级标签筛选器
 * - 第一行：6个大类标签
 * - 第二行：选择大类后动态加载二级标签
 * - 支持关键词搜索
 */
export const TagFilter: React.FC<TagFilterProps>;
```

---

## 状态管理设计

### 认证状态 (Auth Store)

```typescript
// src/features/auth/stores/auth-store.ts

interface AuthState {
  user: User | null;              // 当前用户信息
  currentRole: Role;              // 当前激活角色
  availableRoles: Role[];         // 用户拥有的所有角色
  token: string | null;           // JWT Token
  
  // Actions
  login: (credentials: LoginCredentials) => Promise<void>;
  logout: () => void;
  switchRole: (role: Role) => void;  // 切换角色
  refreshToken: () => Promise<void>;
}
```

### 答题状态 (Exam Store)

```typescript
// src/features/exam/stores/exam-store.ts

interface ExamState {
  currentQuiz: Quiz | null;           // 当前测验
  answers: Record<string, Answer>;    // 用户答案 { questionId: answer }
  currentQuestionIndex: number;       // 当前题目索引
  markedQuestions: string[];          // 标记的题目
  startTime: Date | null;             // 开始时间
  remainingTime: number;              // 剩余时间（秒）
  
  // Actions
  startExam: (quiz: Quiz) => void;
  saveAnswer: (questionId: string, answer: Answer) => void;
  markQuestion: (questionId: string) => void;
  submitExam: () => Promise<ExamResult>;
}
```

---

## API 层设计

### 通用 API 响应类型

```typescript
// src/types/api.ts

interface ApiResponse<T> {
  code: number;
  message: string;
  data: T;
}

interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}
```

### 特性 API 示例

```typescript
// src/features/tasks/api/task-api.ts

import { apiClient } from '@/lib/axios';

/**
 * 任务相关 API
 */
export const taskApi = {
  /** 获取任务列表 */
  getList: (params: TaskListParams) => 
    apiClient.get<PaginatedResponse<Task>>('/api/tasks', { params }),
  
  /** 获取任务详情 */
  getDetail: (id: string) => 
    apiClient.get<Task>(`/api/tasks/${id}`),
  
  /** 创建学习任务 */
  createLearningTask: (data: CreateLearningTaskDto) =>
    apiClient.post<Task>('/api/tasks/learning', data),
  
  /** 创建练习任务 */
  createPracticeTask: (data: CreatePracticeTaskDto) =>
    apiClient.post<Task>('/api/tasks/practice', data),
  
  /** 创建考试任务 */
  createExamTask: (data: CreateExamTaskDto) =>
    apiClient.post<Task>('/api/tasks/exam', data),
};
```

---

## 角色菜单配置

```typescript
// src/config/menus.ts

export const menuConfig: Record<Role, MenuItem[]> = {
  student: [
    { key: 'dashboard', label: '仪表盘', icon: 'DashboardOutlined', path: '/student/dashboard' },
    { key: 'knowledge', label: '知识中心', icon: 'BookOutlined', path: '/student/knowledge' },
    { key: 'tasks', label: '任务中心', icon: 'CheckSquareOutlined', path: '/student/tasks' },
    { key: 'profile', label: '个人中心', icon: 'UserOutlined', path: '/student/profile' },
  ],
  
  mentor: [
    { key: 'dashboard', label: '仪表盘', icon: 'DashboardOutlined', path: '/mentor/dashboard' },
    { 
      key: 'resources', 
      label: '资源管理', 
      icon: 'FolderOutlined',
      children: [
        { key: 'quizzes', label: '测验/试卷管理', path: '/mentor/quizzes' },
      ]
    },
    { key: 'tasks', label: '任务管理', icon: 'ScheduleOutlined', path: '/mentor/tasks' },
    { key: 'grading', label: '评分中心', icon: 'EditOutlined', path: '/mentor/grading' },
    { key: 'spot-check', label: '抽查中心', icon: 'AuditOutlined', path: '/mentor/spot-check' },
  ],
  
  department_manager: [
    // 与导师类似，数据范围不同
  ],
  
  team_manager: [
    { key: 'dashboard', label: '数据看板', icon: 'AreaChartOutlined', path: '/team-manager/dashboard' },
  ],
  
  admin: [
    { key: 'dashboard', label: '仪表盘', icon: 'DashboardOutlined', path: '/admin/dashboard' },
    { 
      key: 'knowledge-mgmt', 
      label: '知识库管理', 
      icon: 'BookOutlined',
      children: [
        { key: 'articles', label: '文章维护', path: '/admin/knowledge' },
        { key: 'tags', label: '标签管理', path: '/admin/knowledge/tags' },
      ]
    },
    { key: 'question-bank', label: '题库管理', icon: 'DatabaseOutlined', path: '/admin/question-bank' },
    { key: 'quizzes', label: '测验管理', icon: 'FormOutlined', path: '/admin/quizzes' },
    { key: 'tasks', label: '任务管理', icon: 'ScheduleOutlined', path: '/admin/tasks' },
    { 
      key: 'user-mgmt', 
      label: '用户与权限', 
      icon: 'TeamOutlined',
      children: [
        { key: 'users', label: '用户列表', path: '/admin/users' },
        { key: 'organization', label: '组织架构', path: '/admin/organization' },
      ]
    },
  ],
};
```

---

## ESLint 规则配置

```javascript
// .eslintrc.cjs

module.exports = {
  rules: {
    'import/no-restricted-paths': [
      'error',
      {
        zones: [
          // 禁止跨特性导入
          { target: './src/features/auth', from: './src/features', except: ['./auth'] },
          { target: './src/features/dashboard', from: './src/features', except: ['./dashboard'] },
          { target: './src/features/knowledge', from: './src/features', except: ['./knowledge'] },
          { target: './src/features/tasks', from: './src/features', except: ['./tasks'] },
          { target: './src/features/quiz', from: './src/features', except: ['./quiz'] },
          { target: './src/features/exam', from: './src/features', except: ['./exam'] },
          { target: './src/features/grading', from: './src/features', except: ['./grading'] },
          { target: './src/features/spot-check', from: './src/features', except: ['./spot-check'] },
          { target: './src/features/question-bank', from: './src/features', except: ['./question-bank'] },
          { target: './src/features/users', from: './src/features', except: ['./users'] },
          { target: './src/features/analytics', from: './src/features', except: ['./analytics'] },
          { target: './src/features/profile', from: './src/features', except: ['./profile'] },
          
          // 禁止特性导入应用层代码
          { target: './src/features', from: './src/app' },
          
          // 禁止共享模块导入特性或应用代码
          {
            target: ['./src/components', './src/hooks', './src/lib', './src/types', './src/utils'],
            from: ['./src/features', './src/app'],
          },
        ],
      },
    ],
  },
};
```

---

## 代码流向图

```
┌─────────────────────────────────────────────────────────────────┐
│                          应用层 (app/)                           │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │ routes  │  │ layouts │  │providers│  │  app.tsx│            │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘            │
└───────┼────────────┼────────────┼────────────┼──────────────────┘
        │            │            │            │
        ▼            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────────┐
│                        特性层 (features/)                        │
│  ┌─────┐ ┌─────────┐ ┌─────────┐ ┌─────┐ ┌────────┐ ┌─────┐   │
│  │auth │ │dashboard│ │knowledge│ │tasks│ │  quiz  │ │ ... │   │
│  └──┬──┘ └────┬────┘ └────┬────┘ └──┬──┘ └───┬────┘ └──┬──┘   │
└─────┼─────────┼───────────┼─────────┼────────┼─────────┼───────┘
      │         │           │         │        │         │
      ▼         ▼           ▼         ▼        ▼         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       共享层                                     │
│  ┌──────────┐ ┌─────┐ ┌────┐ ┌──────┐ ┌─────┐ ┌─────┐         │
│  │components│ │hooks│ │lib │ │stores│ │types│ │utils│         │
│  └──────────┘ └─────┘ └────┘ └──────┘ └─────┘ └─────┘         │
└─────────────────────────────────────────────────────────────────┘

导入规则：
  ✅ 共享层 → 可被任何层导入
  ✅ 特性层 → 可被应用层导入
  ✅ 特性层 → 只能导入共享层
  ❌ 特性层 → 不能导入其他特性
  ❌ 共享层 → 不能导入特性层或应用层
  ❌ 特性层 → 不能导入应用层
```

---

## 开发规范总结

| 规范项 | 说明 |
|--------|------|
| **文件命名** | 使用 kebab-case（如 `user-selector.tsx`） |
| **组件命名** | 使用 PascalCase（如 `UserSelector`） |
| **导入方式** | 使用绝对路径导入（如 `@/components/...`） |
| **状态管理** | 特性级状态放 `features/*/stores`，全局状态放 `stores/` |
| **API 调用** | 封装在 `features/*/api/` 目录，使用统一的 axios 实例 |
| **类型定义** | 特性级类型放 `features/*/types`，共享类型放 `types/` |
| **注释规范** | 使用 JSDoc 注释 |

---

## 快速开始命令

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 代码检查
npm run lint

# 格式化代码
npm run format
```


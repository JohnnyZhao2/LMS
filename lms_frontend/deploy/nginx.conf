server {
    listen 80;
    listen [::]:80;
    server_name lms.example.com;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name lms.example.com;

    ssl_certificate /etc/letsencrypt/live/lms.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/lms.example.com/privkey.pem;

    location = /healthz {
        access_log off;
        default_type application/json;
        return 200 '{"status":"ok","service":"lms-frontend"}';
    }

    location = /readyz {
        access_log off;
        default_type application/json;
        return 200 '{"status":"ok","service":"lms-frontend"}';
    }

    root /srv/LMS/lms_frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
